{
  "traceability_version": 1,
  "scenario_coverage": [
    { "capability": "bpmn-architect-skill", "scenarios": "all", "tests": ["contracts-v2.test.mjs", "build.test.mjs"], "docs": ["SKILL.md", "references/process-yaml.md"] },
    { "capability": "bpmn-collaboration-v2", "scenarios": "all", "tests": ["contracts-v2.test.mjs", "validation.test.mjs"], "docs": ["references/process-yaml.md"] },
    { "capability": "bpmn-semantic-roundtrip", "scenarios": "all", "tests": ["contracts-v2.test.mjs"], "docs": ["references/bpmn-validation.md"] },
    { "capability": "bpmn-validation-reporting", "scenarios": "all", "tests": ["contracts-v2.test.mjs", "schema.test.mjs", "validation.test.mjs"], "docs": ["references/bpmn-validation.md"] },
    { "capability": "corp-bpmn-cli", "scenarios": "all", "tests": ["contracts-v2.test.mjs", "build.test.mjs"], "docs": ["README.md", "references/troubleshooting.md"] },
    { "capability": "process-yaml-model", "scenarios": "all", "tests": ["contracts-v2.test.mjs", "schema.test.mjs", "generation.test.mjs"], "docs": ["references/process-yaml.md", "references/bpmn-authoring.md"] }
  ],
  "evidence": [
    { "id": "capability:unsupported-artifact", "test_file": "contracts-v2.test.mjs", "test": "unsupported data, artifacts, extensions, and multi-participant v1 fail with capability findings" },
    { "id": "capability:unsupported-data", "test_file": "contracts-v2.test.mjs", "test": "unsupported data, artifacts, extensions, and multi-participant v1 fail with capability findings" },
    { "id": "capability:unsupported-extension", "test_file": "contracts-v2.test.mjs", "test": "unsupported data, artifacts, extensions, and multi-participant v1 fail with capability findings" },
    { "id": "capability:unsupported-node", "test_file": "contracts-v2.test.mjs", "test": "unsupported recognized BPMN type fails with capability code and no generator fallback" },
    { "id": "generation:activities", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "generation:basic", "test_file": "generation.test.mjs", "test": "generated BPMN parses with bpmn-moddle" },
    { "id": "generation:condition", "test_file": "generation.test.mjs", "test": "sequence flow condition survives generation and BPMN parse-back" },
    { "id": "generation:default-flow", "test_file": "generation.test.mjs", "test": "gateway default target and flow references generate the same BPMN default flow" },
    { "id": "generation:events", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "generation:gateway", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "generation:subprocess", "test_file": "generation.test.mjs", "test": "subprocess generates BPMN XML" },
    { "id": "generation:tasks", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "generation:transaction", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "generation:v1-participant", "test_file": "contracts-v2.test.mjs", "test": "generation and semantic projections are deterministic" },
    { "id": "generation:v2-collaboration", "test_file": "contracts-v2.test.mjs", "test": "v2 node and participant message flows preserve distinct process ownership" },
    { "id": "generation:v2-message-flow", "test_file": "contracts-v2.test.mjs", "test": "v2 node and participant message flows preserve distinct process ownership" },
    { "id": "references:default-flow", "test_file": "schema.test.mjs", "test": "canonical v1 rejects an outgoing target node as a default reference" },
    { "id": "references:process-ownership", "test_file": "contracts-v2.test.mjs", "test": "v2 rejects missing or shared participant process ownership" },
    { "id": "roundtrip:boundary", "test_file": "contracts-v2.test.mjs", "test": "condition language, default flow, boundary attachment, and documentation round-trip" },
    { "id": "roundtrip:condition", "test_file": "contracts-v2.test.mjs", "test": "round-trip reports semantic loss even when altered XML remains parseable" },
    { "id": "roundtrip:default", "test_file": "contracts-v2.test.mjs", "test": "condition language, default flow, boundary attachment, and documentation round-trip" },
    { "id": "roundtrip:events", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "roundtrip:nodes", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "roundtrip:v1-participant", "test_file": "contracts-v2.test.mjs", "test": "generation and semantic projections are deterministic" },
    { "id": "roundtrip:v2-collaboration", "test_file": "contracts-v2.test.mjs", "test": "v2 node and participant message flows preserve distinct process ownership" },
    { "id": "roundtrip:v2-message-flow", "test_file": "contracts-v2.test.mjs", "test": "v2 node and participant message flows preserve distinct process ownership" },
    { "id": "schema:boundary-attachment", "test_file": "contracts-v2.test.mjs", "test": "event definitions and boundary attachments are rejected on incompatible shapes" },
    { "id": "schema:boundary-forward-ref", "test_file": "schema.test.mjs", "test": "advanced boundary event is accepted by schema" },
    { "id": "schema:condition-shape", "test_file": "contracts-v2.test.mjs", "test": "strict schema rejects unknown fields and canonical string conditions" },
    { "id": "schema:event-definition", "test_file": "contracts-v2.test.mjs", "test": "all supported node and event-definition variants generate and round-trip" },
    { "id": "schema:event-shape", "test_file": "contracts-v2.test.mjs", "test": "event definitions and boundary attachments are rejected on incompatible shapes" },
    { "id": "schema:gateway-shape", "test_file": "schema.test.mjs", "test": "canonical v1 rejects an outgoing target node as a default reference" },
    { "id": "schema:node-type", "test_file": "contracts-v2.test.mjs", "test": "all advertised supported node variants satisfy their strict schema shape" },
    { "id": "schema:v1-multiple-participants", "test_file": "contracts-v2.test.mjs", "test": "unsupported data, artifacts, extensions, and multi-participant v1 fail with capability findings" },
    { "id": "semantic:gateway-branches", "test_file": "validation.test.mjs", "test": "exclusive gateway without outgoing labels is an error" },
    { "id": "semantics:message-boundary", "test_file": "validation.test.mjs", "test": "message flow inside one participant is an error" }
  ]
}
